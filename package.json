{
  "name": "opencode-dolphinscheduler-plugin",
  "version": "0.1.0",
  "description": "Apache DolphinScheduler plugin for OpenCode: multi-tenant scheduled agent tasks with asynchronous Master callback dispatch.",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "zod": "^3.23.8"
  },
  "peerDependencies": {
    "@opencode-ai/plugin": ">=1.0.0"
  },
  "peerDependenciesMeta": {
    "@opencode-ai/plugin": {
      "optional": true
    }
  },
  "devDependencies": {
    "@types/bun": "^1.2.0",
    "typescript": "^5.6.3"
  }
}
